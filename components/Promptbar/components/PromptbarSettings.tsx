// Importing the FC (Functional Component) type from React
import { FC } from 'react';

// Interface for the component's props (currently empty)
interface Props {}

// Functional component named PromptbarSettings, which takes no props
export const PromptbarSettings: FC<Props> = () => {
  // Rendering an empty <div> for now, you may later add content or functionality
  return <div></div>;
};

